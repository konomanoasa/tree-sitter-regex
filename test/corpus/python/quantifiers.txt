==================
simple interval and modified quantifiers share one repetition shape
:language(python_re)
:language(python_re_verbose)
==================
a*b+?c?+d{2}e{2,4}?f{2,}+g{,4}h{,}\

---

(pattern
  body: (alternation
    (concatenation
      (repetition
        operand: (literal_character)
        quantifier: (quantifier))
      (repetition
        operand: (literal_character)
        quantifier: (quantifier))
      (repetition
        operand: (literal_character)
        quantifier: (quantifier))
      (repetition
        operand: (literal_character)
        quantifier: (quantifier
          count: (decimal_digits)))
      (repetition
        operand: (literal_character)
        quantifier: (quantifier
          minimum: (decimal_digits)
          maximum: (decimal_digits)))
      (repetition
        operand: (literal_character)
        quantifier: (quantifier
          minimum: (decimal_digits)))
      (repetition
        operand: (literal_character)
        quantifier: (quantifier
          maximum: (decimal_digits)))
      (repetition
        operand: (literal_character)
        quantifier: (quantifier))
      (literal_escape))))

==================
representative consuming atom categories can be quantified
:language(python_re)
:language(python_re_verbose)
==================
.*\d+[ab]?(?x:c){2}\1+\

---

(pattern
  body: (alternation
    (concatenation
      (repetition
        operand: (wildcard)
        quantifier: (quantifier))
      (repetition
        operand: (character_class_escape)
        quantifier: (quantifier))
      (repetition
        operand: (character_class
          (class_character)
          (class_character))
        quantifier: (quantifier))
      (repetition
        operand: (scoped_flags_group
          enable: (flag_set)
          body: (alternation
            (concatenation
              (literal_character))))
        quantifier: (quantifier
          count: (decimal_digits)))
      (repetition
        operand: (numeric_backreference
          group: (group_id))
        quantifier: (quantifier))
      (literal_escape))))

==================
quantifier bounds retain source spelling without semantic validation
:language(python_re)
:language(python_re_verbose)
==================
a{0002}b{4,2}c{12345678901234567890}\

---

(pattern
  body: (alternation
    (concatenation
      (repetition
        operand: (literal_character)
        quantifier: (quantifier
          count: (decimal_digits)))
      (repetition
        operand: (literal_character)
        quantifier: (quantifier
          minimum: (decimal_digits)
          maximum: (decimal_digits)))
      (repetition
        operand: (literal_character)
        quantifier: (quantifier
          count: (decimal_digits)))
      (literal_escape))))

==================
comment group layout joins the following quantifier relation only
:language(python_re)
:language(python_re_verbose)
==================
a(?#gap)b(?#gap)*\

---

(pattern
  body: (alternation
    (concatenation
      (literal_character)
      (comment_group)
      (repetition
        operand: (literal_character)
        (comment_group)
        quantifier: (quantifier))
      (literal_escape))))

==================
initial verbose layout belongs to the following quantifier relation
:language(python_re_verbose)
==================
a # gap
 *\

---

(pattern
  body: (alternation
    (concatenation
      (repetition
        operand: (literal_character)
        (verbose_whitespace)
        (verbose_comment)
        (verbose_whitespace)
        quantifier: (quantifier))
      (literal_escape))))

==================
scoped verbose layout belongs to the following quantifier relation
:language(python_re)
:language(python_re_verbose)
==================
(?x:a # gap
 *)\

---

(pattern
  body: (alternation
    (concatenation
      (scoped_flags_group
        enable: (flag_set)
        body: (alternation
          (concatenation
            (repetition
              operand: (literal_character)
              (verbose_whitespace)
              (verbose_comment)
              (verbose_whitespace)
              quantifier: (quantifier)))))
      (literal_escape))))

==================
incomplete interval-like spellings remain ordinary characters
:language(python_re)
:language(python_re_verbose)
==================
a{2|b{2,|c{2,3,}|d{}|e}f\

---

(pattern
  body: (alternation
    (concatenation
      (literal_character)
      (literal_character)
      (literal_character))
    (concatenation
      (literal_character)
      (literal_character)
      (literal_character)
      (literal_character))
    (concatenation
      (literal_character)
      (literal_character)
      (literal_character)
      (literal_character)
      (literal_character)
      (literal_character)
      (literal_character))
    (concatenation
      (literal_character)
      (literal_character)
      (literal_character))
    (concatenation
      (literal_character)
      (literal_character)
      (literal_character)
      (literal_escape))))

==================
quantifier punctuation remains ordinary inside a character class
:language(python_re)
:language(python_re_verbose)
==================
[*+?{}]\

---

(pattern
  body: (alternation
    (concatenation
      (character_class
        (class_character)
        (class_character)
        (class_character)
        (class_character)
        (class_character))
      (literal_escape))))
