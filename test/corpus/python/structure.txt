==================
expression hierarchy and basic atoms in normal mode
:language(python_re)
==================
ab|.^$-

---

(pattern
  body: (alternation
    (concatenation
      (literal_character)
      (literal_character))
    (concatenation
      (wildcard)
      (start_anchor)
      (end_anchor)
      (literal_character)
      (literal_character))))

==================
expression hierarchy and basic atoms in verbose mode
:language(python_re_verbose)
==================
ab|.^$-

---

(pattern
  body: (alternation
    (concatenation
      (literal_character)
      (literal_character))
    (concatenation
      (wildcard)
      (start_anchor)
      (end_anchor)
      (literal_character)
      (verbose_whitespace))))

==================
empty alternatives retain a normal trailing newline
:language(python_re)
==================
|a|

---

(pattern
  body: (alternation
    (concatenation
      (literal_character))
    (concatenation
      (literal_character))))

==================
empty alternatives retain a verbose trailing newline
:language(python_re_verbose)
==================
|a|

---

(pattern
  body: (alternation
    (concatenation
      (literal_character))
    (concatenation
      (verbose_whitespace))))

==================
comment group is normal-mode prefix layout
:language(python_re)
==================
(?# comment | # (? )a

---

(pattern
  (comment_group)
  body: (alternation
    (concatenation
      (literal_character)
      (literal_character))))

==================
comment group is verbose-mode prefix layout
:language(python_re_verbose)
==================
(?# comment | # (? )a

---

(pattern
  (comment_group)
  body: (alternation
    (concatenation
      (literal_character)
      (verbose_whitespace))))

==================
closing bracket is ordinary outside a character class
:language(python_re)
:language(python_re_verbose)
==================
a]b\

---

(pattern
  body: (alternation
    (concatenation
      (literal_character)
      (literal_character)
      (literal_character)
      (literal_escape))))
