==================
class boundaries preserve positional punctuation
:language(python_re)
:language(python_re_verbose)
==================
[^a][]][-a][a-]\

---

(pattern
  body: (alternation
    (concatenation
      (character_class
        (class_character))
      (character_class
        (class_character))
      (character_class
        (class_character)
        (class_character))
      (character_class
        (class_character)
        (class_character))
      (literal_escape))))

==================
ranges bind from left to right
:language(python_re)
:language(python_re_verbose)
==================
[a-z][]-a][--a][a-b-c][a--b]\

---

(pattern
  body: (alternation
    (concatenation
      (character_class
        (class_range
          start: (class_character)
          end: (class_character)))
      (character_class
        (class_range
          start: (class_character)
          end: (class_character)))
      (character_class
        (class_range
          start: (class_character)
          end: (class_character)))
      (character_class
        (class_range
          start: (class_character)
          end: (class_character))
        (class_character)
        (class_character))
      (character_class
        (class_range
          start: (class_character)
          end: (class_character))
        (class_character))
      (literal_escape))))

==================
class escapes expose class-specific nodes and fields
:language(python_re)
:language(python_re_verbose)
==================
[\b\1\12\1234\d\a-\x42]\

---

(pattern
  body: (alternation
    (concatenation
      (character_class
        (backspace_escape)
        (octal_escape
          digits: (octal_digits))
        (octal_escape
          digits: (octal_digits))
        (octal_escape
          digits: (octal_digits))
        (class_character)
        (character_class_escape)
        (class_range
          start: (control_escape)
          end: (hex_escape
            digits: (hexadecimal_digits))))
      (literal_escape))))

==================
str profiles allow Unicode escapes as range endpoints
:language(python_re)
:language(python_re_verbose)
==================
[\u0041-\N{B}]\

---

(pattern
  body: (alternation
    (concatenation
      (character_class
        (class_range
          start: (unicode_escape
            digits: (hexadecimal_digits))
          end: (named_unicode_escape
            name: (unicode_character_name))))
      (literal_escape))))

==================
nested-set warning spellings retain current class structure
:language(python_re)
:language(python_re_verbose)
==================
[[a][a&&b][a~~b][a||b][a--b]\

---

(pattern
  body: (alternation
    (concatenation
      (character_class
        (class_character)
        (class_character))
      (character_class
        (class_character)
        (class_character)
        (class_character)
        (class_character))
      (character_class
        (class_character)
        (class_character)
        (class_character)
        (class_character))
      (character_class
        (class_character)
        (class_character)
        (class_character)
        (class_character))
      (character_class
        (class_range
          start: (class_character)
          end: (class_character))
        (class_character))
      (literal_escape))))

==================
set escapes do not become range endpoints
:language(python_re)
:language(python_re_verbose)
==================
[\d-a][a-\d]\

---

(pattern
  body: (alternation
    (concatenation
      (character_class
        (character_class_escape)
        (class_character)
        (class_character))
      (character_class
        (class_character)
        (class_character)
        (character_class_escape))
      (literal_escape))))

==================
verbose mode keeps class whitespace and comment spellings as members
:language(python_re)
:language(python_re_verbose)
==================
(?x:[ (?#|)
])\

---

(pattern
  body: (alternation
    (concatenation
      (scoped_flags_group
        enable: (flag_set)
        body: (alternation
          (concatenation
            (character_class
              (class_character)
              (class_character)
              (class_character)
              (class_character)
              (class_character)
              (class_character)
              (class_character)))))
      (literal_escape))))
